const colors = {
  mainColor: '#feff67',
  mainHoverColor: '#353535',
  black: '#000000',
  white: '#ffffff',
  gray: '#313131',
};

const size = {
  maxWidth: 1920,
  maxHeight: 1080,
};

const theme = {
  colors,
  size,
};

export default theme;
